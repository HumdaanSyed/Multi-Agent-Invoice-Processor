"""Placeholder test so `pytest` has something to collect from Phase 0 onward.

Replace/expand as real modules land (schema validation, extraction, graph
routing, etc. in later phases).
"""


def test_package_imports():
    import invoice_agent  # noqa: F401
